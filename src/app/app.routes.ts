import { Routes } from '@angular/router';
import { ProductListComponent } from './features/products/product.component';

export const routes: Routes = [
    {path: '', component: ProductListComponent}
];
